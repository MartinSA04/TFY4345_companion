// Re-export the framework's content collections verbatim. This is the ONLY file
// in src/ — the course owns no pages, components, or schema of its own.
export { collections } from "study-companion/content";
